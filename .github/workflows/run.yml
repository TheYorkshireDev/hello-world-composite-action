on:
  pull_request:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v3

      - id: foo
        uses: ./

      - id: bar
        uses: ./
        with:
          terraform_docs_version: 'v0.14.0'
